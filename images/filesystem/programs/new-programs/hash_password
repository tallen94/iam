import json
import hashlib
import base64
import hashlib, binascii

args = raw_input()
data = json.loads(args)

salt = "asdfasdfasdfasdf"
dk = hashlib.pbkdf2_hmac('sha256', data["password"], salt, 100000)
data["password"] = binascii.hexlify(dk)

out = json.dumps(data)
print out

