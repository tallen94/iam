import json
import uuid

args = raw_input()
data = json.loads(args)

if data["user"]:
    out = { "token": str(uuid.uuid4()), "userId": data["user"]["id"], "username": data["user"]["username"] }
else:
    out = { "error": data["error"] }
    
print json.dumps(out)