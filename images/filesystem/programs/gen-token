import json
import uuid

args = raw_input()
data = json.loads(args)
user = data[1][0]

if user:
    out = { "token": str(uuid.uuid4()), "userId": user["id"], "username": user["username"] }
else:
    out = { "error": data["error"] }

print json.dumps(out)