import json
import requests

args = input()
data = json.loads(args)
endpoints = requests.post("http://iam-router.default/executable/admin/function/kubectl-ep/run", data={
    "svc": data["svc"]
}).json()["result"].strip().split(",")

print(json.dumps({"endpoints": endpoints}))
