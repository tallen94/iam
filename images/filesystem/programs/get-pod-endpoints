import requests
import json

args = input()
data = json.loads(args)

response = requests.post("http://iam-router.default/executable/admin/function/kubectl-ep/run", data={
    "name": data["name"]
}).json()

endpoints = response["result"].strip().split(",")

out={"endpoints": endpoints}
print(json.dumps(out))