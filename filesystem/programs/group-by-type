import json

args = raw_input()
data = json.loads(args)
groups = {}
for item in data:
    if not item["type"] in groups:
        groups[item["type"]] = []
    groups[item["type"]].append(item)

print json.dumps(groups)