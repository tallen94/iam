name: Build Base Pull Request

on: 
  pull_request:
    paths:
    - 'apps/dashboard/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - uses: actions/setup-node@v1
      with:
        node-version: '11.14.0'

    - name: Build Dashboard
      run: bash apps/dashboard/build.sh -prod