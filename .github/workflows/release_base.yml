name: Release Base

on: 
  push:
    branches:
    - 'release/base'

jobs:
  build:
    container: opctl:0.1.45-dind
    steps:
    - uses: actions/checkout@v1
    - name: Docker login
      run: docker login -u ${{secrets.DOCKER_USERNAME}} -p ${{secrets.DOCKER_PASSWORD}}
    
    - name: Build Base
      run: opctl run -a dockerSocket="/var/run/docker.sock" -a version="$GITHUB_SHA" -a push="true"
    
    - name: Commit Changes
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git checkout -b deploy/base-${{ github.sha }}
        git commit -m "Deploy Request for base ${{ github.sha }}" -a
    - name: Push changes
      uses: ad-m/github-push-action@master
      with:
        branch: deploy/base-${{ github.sha }}
        github_token: ${{ secrets.ACTIONS_TOKEN }}
